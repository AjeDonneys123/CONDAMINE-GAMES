{
  "name": "condamine-games",
  "version": "1.0.0",
  "private": true,
  "description": "Jeux éducatifs de CondaWeb, déployés ensemble sur Vercel.",
  "scripts": {
    "build:wispguard": "npm --prefix games/wispguard ci --no-audit --no-fund && npm --prefix games/wispguard run build",
    "build": "npm run build:wispguard && node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}

